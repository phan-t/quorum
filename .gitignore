node_modules/
dist/
.env
.env.*
!.env.example

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# .terraform.lock.hcl is deliberately NOT ignored: it pins provider hashes so
# a run in HCP Terraform resolves what was tested here, not whatever is newest.

# macOS
.DS_Store
